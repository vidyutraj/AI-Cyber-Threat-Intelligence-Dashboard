requests>=2.32.0
curl-cffi
flask>=3.0.0
flask-cors>=5.0.0
beautifulsoup4>=4.12.0
openai>=1.6.0
# intel/ package uses only stdlib (sqlite3, hashlib, math, etc.).
# External enrichment APIs (NVD, FIRST EPSS, CISA KEV) are reached via requests above.
